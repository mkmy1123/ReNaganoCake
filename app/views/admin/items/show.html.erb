<h3>商品詳細</h3>
<%= attachment_image_tag @item, :item_image %><br>
<p>商品名 <strong><%= @item.name %></strong></p>
<p>商品説明 <strong><%= @item.description %></strong></p>
<p>ジャンル <strong><%= @genre.name %></strong></p>
<p>税込価格（税抜価格） <strong><%= (@item.price * 1.1).floor %>（<%= @item.price %>）</strong>円</p>
<p>販売ステータス
	<%if @item.is_selling == true %>
		<strong>販売中</strong>
	<% else %>
		<strong>販売停止中</strong>
	<% end %>
</p>
<%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-primary" %>