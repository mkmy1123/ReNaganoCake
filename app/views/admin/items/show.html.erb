<div class="container" style="padding-top: 50px">
<h2 style="margin-bottom: 30px; padding-right: 89%; font-weight: bold">商品詳細</h2>

<table class="table">
<tr class="active" style="font-weight: bold">
<td>商品イメージ</td>
<td>商品名</td>
<td>商品説明</td>
<td>ジャンル</td>
<td>税込価格（税抜価格)</td>
<td>販売ステータス</td>
<td></td>
</tr><br>

<tr>
<td><%= attachment_image_tag @item, :item_image %></td>
<td><%= @item.name %></td>
<td><%= @item.description %></td>
<td><%= @genre.name %></td>
<td><%= (@item.price * 1.1).floor %>（<%= @item.price %>)円</td>
	<%if @item.is_selling == true %>
	    <td>販売中</td>
	<% else %>
		<td>販売停止中</td>
	<% end %>

 <td><%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-primary" %></td>
</tr>
</table>

</div>